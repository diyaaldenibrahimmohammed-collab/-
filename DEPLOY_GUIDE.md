# 🚀 خطوات رفع وتشغيل بوت الواتساب على السيرفر

هذا الدليل يشرح كيفية رفع مجلد البوت (`whatsapp_api_bot_project`) إلى سيرفرك وتشغيله ليعمل 24 ساعة ويرتبط بالدومين `bot.wassili.site`.

---

## 1. تجهيز الملفات (على جهازك)
1. تأكد من أن ملف `.env` يحتوي على البيانات الصحيحة.
2. **لا ترفع** مجلد `node_modules` أو `sessions` (سننشئها هناك).
3. اضغط مجلد `whatsapp_api_bot_project` بالكامل في ملف **ZIP**.

---

## 2. الرفع للسيرفر
1. افتح **cPanel** أو اتصل بـ **FTP/SFTP**.
2. ارفع ملف الـ ZIP إلى مجلد رئيسي (مثلاً بجانب `public_html` وليس بداخله، ليكون محمياً).
3. فك الضغط (Extract). ليكن المسار مثلاً: `/home/USERNAME/whatsapp_bot`.

---

## 3. التثبيت (من التيرمينال / SSH)
اتصل بالسيرفر عبر SSH ونفذ الأوامر التالية:

```bash
# اذهب لمجلد البوت
cd /home/USERNAME/whatsapp_bot

# تثبيت المكتبات
npm install

# تثبيت متصفح كروم المخصص للسيرفر
npx puppeteer browsers install chrome
```

---

## 4. التشغيل المستمر (باستخدام PM2)
نستخدم `pm2` لضمان عمل البوت في الخلفية وإعادة تشغيله تلقائياً لو توقف.

```bash
# تثبيت PM2 (إذا لم يكن مثبتاً)
npm install -g pm2

# تشغيل البوت (OTP Only)
# اسميناه "otp-bot" لسهولة التعرف عليه
pm2 start index-otp-only.js --name "otp-bot"

# حفظ الحالة ليعمل بعد إعادة تشغيل السيرفر
pm2 save
pm2 startup
```

---

## 5. ربط الدومين (bot.wassili.site)
لجعل هذا الدومين يفتح صفحة الـ QR (التي تعمل على `localhost:3000` داخل السيرفر)، نحتاج لعمل **Reverse Proxy**.

### إذا كنت تستخدم cPanel:
1. اذهب إلى **Domains** وتأكد أن `bot.wassili.site` موجود.
2. (صعب قليلاً في cPanel العادي يتطلب Node.js App Setup). الأسهل استخدام **Nginx** إذا كان لديك صلاحية `root`.

### إذا كنت تستخدم Nginx (صلاحية Root):
أنشئ ملف إعداد جديد في `/etc/nginx/sites-available/bot.wassili.site`:

```nginx
server {
    server_name bot.wassili.site;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```
ثم فعّله:
```bash
ln -s /etc/nginx/sites-available/bot.wassili.site /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

---

## 6. الخطوة الأخيرة: الربط
1. افتح في متصفحك: `https://bot.wassili.site/qr`
2. امسح الكود بواتساب الخاص بالإشعارات.
3. ✅ **مبروك!** البوت يعمل الآن على السيرفر ويرسل الأكواد لتطبيقك الذي يتصل به داخلياً عبر `http://localhost:3000`.
